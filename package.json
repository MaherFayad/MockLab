{
  "name": "mocklab",
  "version": "1.0.0",
  "private": true,
  "description": "Click any element, find the data behind it, change it, and see the site render the new state.",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=20" },
  "workspaces": ["extension", "companion"],
  "scripts": {
    "test": "npm test -ws",
    "zip": "rm -f mocklab-extension.zip && cd extension && zip -qr ../mocklab-extension.zip . -x 'test/*' 'testlib/*' 'package.json' '*.DS_Store' && cd .. && unzip -l mocklab-extension.zip | tail -1"
  }
}
