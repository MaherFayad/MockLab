/**
 * CRUD for Changes / Scenarios / Bindings + the chrome.storage.local schema.
 *
 * OWNER: interceptor-engineer. Implements PLAN.md §4 (storage schema), §5.3.
 * Filled in at milestone M1/M2. Kept as a module from M0 so the file tree in
 * PLAN.md §2.1 is stable and no import path ever moves.
 */
export {};
