/**
 * Deep mode: chrome.debugger + CDP Fetch attach/detach and document rewriting.
 *
 * OWNER: probe-engineer. Implements PLAN.md §8.
 * Filled in at milestone M7. Kept as a module from M0 so the file tree in
 * PLAN.md §2.1 is stable and no import path ever moves.
 */
export {};
