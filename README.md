# MockLab

**Click any element on any website, find the data behind it, change it, and watch the
site render the new state.**

MockLab is a Chrome extension. Pick a price, a status pill, or a name on a page, and
MockLab works out which piece of loaded data controls it — by experiment, not by
guessing — then lets you change that value. The website's own code does the rendering,
so what you see is exactly what real users would see.

Nothing leaves your computer. No accounts, no servers, no tracking.

---

## 1. Install the extension

1. Open Chrome and go to `chrome://extensions`
2. Turn on **Developer mode** (top-right switch)
3. Click **Load unpacked** and choose the `extension` folder from this project

_[screenshot: chrome://extensions with Developer mode on]_

## 2. Pin it and open a site

Click the puzzle-piece icon in Chrome's toolbar and pin **MockLab**. Open any website,
then click the MockLab icon — the panel opens beside the page.

_[screenshot: the MockLab panel open next to a website]_

## 3. Optional — let an AI assistant drive it

This step is only needed if you want Claude Code (or another AI assistant) to read and
change data for you. MockLab works fully without it.

1. Install Node.js from [nodejs.org](https://nodejs.org) (any version 20 or newer).
2. Once, in this project's folder, run `npm install`.
3. **Pair your browser.** In a terminal, run this and leave it running:

   ```
   node /path/to/MockLab/companion/src/index.js --pair
   ```

   It prints a 6-digit code. Open MockLab's **Settings** tab, choose **Set up AI
   access**, and type the code in within 5 minutes. Then press `Ctrl+C` to stop it.

4. **Hand it to your assistant.**

   ```
   claude mcp add mocklab -- node /path/to/MockLab/companion/src/index.js --stdio
   ```

Worth knowing, because guides for other tools will tell you otherwise:

- MockLab is not on npm yet, so the shorter `npx mocklab-companion` form will not find
  it. The full path is what works today.
- Pair in step 3 **before** step 4, not after. Once your assistant is launching MockLab
  itself, the 6-digit code goes to your assistant's log rather than to a terminal you
  are looking at. Pairing is remembered, so you only ever do it once per browser.
- Only one copy can run at a time — they share a port. Stop the one from step 3 before
  your assistant starts its own.

_[screenshot: the pairing code screen]_

---

## Try it safely first

MockLab ships with a small practice website so you can learn the tool without touching
anything real.

```
npx mocklab-companion
```

Then open **http://127.0.0.1:8517/demo/** — a pretend airline booking page with a
flight status, a price, and a passenger name. Try picking the status pill and changing
it to `CANCELLED`: the pill turns red and a cancellation banner appears, because the
page's own code reacted to the new data.

---

## What MockLab can't do yet

Being honest about the edges matters more than sounding capable:

- **Content inside frames.** Only the main page is covered in version 1.
- **Live-updating data** (WebSockets, live event streams) and **modern streamed pages**
  (React Server Components) are listed as sources but can't be edited. MockLab passes
  them through untouched and never reads them, so live tickers, chat views and
  streaming pages keep working normally with MockLab installed — they just show up
  greyed out rather than editable.
- **Sites that load data only once per visit.** Your change applies the next time the
  site asks for that data, not on a plain refresh.
- **Chromium browsers only** — Chrome, Edge, Brave, Arc. Not Firefox or Safari.
- **Non-text data formats** (binary, protobuf) are listed but can't be edited.
- **Screenshots** capture the visible part of the page, not the full scrollable page.

---

## Deviations from PLAN.md

Recorded per PLAN.md §17.11 — where the build differs from the specification and why.

| # | Milestone | Deviation | Why |
|---|-----------|-----------|-----|
| 1 | M0 | **`"sidePanel"` added to `manifest.json` permissions.** PLAN.md §3 says to write the manifest verbatim, and its permission array omits it. | `chrome.sidePanel` is undefined without this permission, so the toolbar icon can never open the panel — §3's own Notes require exactly that behaviour. Verified in Chromium: without the permission the namespace is `undefined`; with it, `setPanelBehavior` succeeds. `background.js` now checks for the namespace explicitly rather than optional-chaining, because the optional-chained form failed silently with a clean console. §17.11: prefer the working behaviour. |
| 2 | M0 | The demo page fetches `api/trip.json` with `fetch()` but `api/user.json` with `XMLHttpRequest`. PLAN.md §14 describes both as fetches. | The demo is the acceptance harness for every milestone. Using both APIs means it exercises both interception paths in §5.1 — the `fetch` wrapper and the harder `responseText` override — instead of leaving the XHR path untested through M6. Still two sources, as §14 requires. |
| 3 | M0 | The demo page loads no web font; it uses Inter when the machine has it and falls back to the system UI face. PLAN.md §14 specifies Inter. | A probe run reloads the page up to eight times. A web-font request on every load would appear in the capture list and delay the network-quiet condition in §7.3's settle definition. The panel will import Inter per §9.1 when `panel.css` is completed at M2 (see BUILD_LOG, "Carried into M2"). |
| 4 | M0 | The demo shows a banner for `DELAYED`, not only for `CANCELLED` as §14 specifies. | Gives the probe's inverse discovery (§7.6) a second multi-element case to find, so `elements[]` is exercised for more than one status value. Costs nothing and makes the harness stricter. |
| 5 | M0 | The demo's rotating tip box also prints `Gate {flight.gate}`, which §14 does not mention. | The noisy box needs to yield real value-match candidates, otherwise M4's `probe.tooNoisy` DoD cannot be tested — picking a box that matches nothing would fail with "no candidates" instead of exercising the noise mask. |
| 6 | M0 | The demo server answers `/favicon.ico` with `204` instead of `404`. | Chrome requests a favicon on every load. A 404 puts a red error in the page console, and the demo's console is where every later milestone's acceptance is judged. |
| 7 | M1 | **§5.2's volatile-param rule extended to delimited tokens inside a param name.** The spec's list is anchored, so it only fires when the volatile word IS the whole name. MockLab also drops a param when a volatile marker appears as a delimited token (split on `_ - .` and camelCase). | The product owner's real target page sends `masterhotelid_tracelogid=100051355-0a8e3544-496571-67667`, which the anchored list misses, and whose value escapes every volatile-value rule too (the dashes defeat all four). Under a literal reading the identity of that request would change on every single page load and **no change could ever apply** — the product would silently do nothing on the page it exists to demo. The token set used for this is deliberately narrower than the anchored list: generic words like `t`, `time`, `sign` and `hash` are excluded, so `checkInTime` and `signInMethod` are still treated as meaningful. Proved on a live page: a fresh trace id per load yields one stable identity, and a change made on one load still applies on the next. |
| 8 | M1 | **A fifth storage key family, `signatures:<origin>`.** PLAN.md §4 lists four. | A change is keyed by request identity, but the in-page matcher works on method and URL, so the service worker must be able to turn an identity back into a URL shape at page start — before that load has captured anything. Without this cache the match list is empty on every fresh load and no change could apply, which is M2's whole point. Captured **responses** stay session-only exactly as §4 requires; only the tiny identity shape is stored. |
| 9 | M1 | **The compiled match list carries query constraints beside the regex, not inside it.** §5.2 describes one `urlRegex` built from the whole normalized URL; MockLab builds it from the origin+path half and passes the query as a `params` list. | A normalized URL has its volatile params dropped and the rest sorted, so a regex built from it could never match the concrete, unsorted URL a page actually requests. Matching params by name and value is what that normalization means: "these must be present with these values; anything else is volatile". Entries are ordered most-constrained-first so §5.3's "first match wins" picks the most specific one. |
| 10 | M1 | **The page's first requests wait for the match list** (hard cap 1 second, once per page load). §5.1.5 says the change table is pushed in so application is synchronous. | It is pushed in — but that takes a few milliseconds, and a page that fires its data requests immediately gets its response back before any change is known, so the edit silently does nothing. The demo's XHR lost this race consistently. §5.1.5's actual hazard is an async round-trip **per request**; this waits once, for the table, and every request afterwards is fully synchronous. Measured cost, 11 loads per condition, two independent runs: demo time-to-render median 60 / 56 ms without the extension and 59 / 55 ms with it — the wait is inside the page's own start-up and does not show above the noise. It IS visible on a request issued in the page's very first milliseconds: an event stream's first chunk arrives at 13 ms without the extension and 43-49 ms with it, and the identical delta appears on a binary response MockLab never even reads, which is what shows it is this one-time wait rather than a per-response cost. |
| 11 | M1 | **`interceptor.js` is 920 lines (665 of them code), above §17.10's ~500.** | §17.10 says "split when bigger". Splitting it is technically possible — the manifest accepts several `js` entries in one MAIN-world block — but those entries **share the page's own global scope**, so the pieces could only reach each other through `window`. That would publish MockLab's match list and patch internals on a hostile page's global object, where any site script could read or rewrite them; the current single closure keeps everything private. A bundler is the other way to split, and §17.10 forbids that too. Everything that can live outside the page already does, in `signatures.js`, `jsonpath.js` and `ruleStore.js`. |
| 12 | M1 | **Both content scripts duplicate a few constants from `messages.js`**, not just `interceptor.js` as the M0 note anticipated. | Content scripts are classic scripts: `import` is a syntax error in `agent.js` too, and dynamic `import()` of an extension file would require making `messages.js` web-accessible to every page. Both files carry a clearly-marked mirrored block, and `messages.js`'s header names both. |
| 13 | M1 | **The XHR body is swapped through instance-level `responseText` / `response` getters installed at `open()`, not §5.1.3's capture-phase `readystatechange` listener.** | §5.1.3 assumes "capture listeners registered first fire first". That is a DOM-tree rule; `XMLHttpRequest` is not a tree, so listeners run in registration order and `{capture:true}` changes nothing. A site that assigns `onreadystatechange` before calling `send()` would therefore read the real body before MockLab's listener ever ran, and the change would apply at random. A lazy getter cannot lose that race: whoever reads the body first triggers the swap. |
| 14 | M1 | **A response body is only buffered when a change actually matches it.** §5.1.2 reads as "always clone and read the body, then decide". | Matching needs the method, the URL and the *request* body — never the response body — so the decision can be made first. Reading first is not merely slower, it is a page-breaking bug: an open event stream never finishes, so awaiting its body meant the site's `fetch()` promise never resolved and any live ticker or chat view froze permanently, on every site, with zero changes configured. Streamed content types are now never read or even cloned (listed read-only per §5.1.4), and everything else is read *after* the original response has already gone back to the page. Verified against the same page with the extension off and on, three runs: the first live chunk arrives at 13-14 ms without the extension and 43-49 ms with it, and `text/x-component` headers at ~110 ms vs ~145 ms — where before the fix the live stream never arrived at all and `text/x-component` took 1527 ms, the full duration of the stream. The remaining delta is deviation 10's one-time wait: it is the same on a binary response MockLab never reads. |
| 15 | M1 | **`extension/test/e2e.browser.test.js` added**, which PLAN.md §2.1's file tree does not list, plus a second CI job that installs Playwright and runs it. | Every M1 defect that mattered was invisible to unit tests and only appeared in a real browser: a streamed response that froze the page, a compiled match entry that could not match its own URL, and a stream MockLab never released. A guard that lives outside the repository cannot fire for anyone else. The suite loads the genuine unpacked extension in Chromium and asserts against the genuine service worker and MAIN-world patch. It **skips itself** when Playwright or a Chromium build is missing, so `npm test -ws` stays green on a plain Node machine. All fixtures live inside the one file because `node --test` executes every `.js` file under `test/`, so a separate fixture module would be run as a test. |
| 16 | M1 | **A change is dropped, not applied, when the response it targets takes longer than 3 seconds to arrive** — and the capture is flagged `changeDropped` so the panel can say so. | Rewriting a body means buffering it, and a body that never ends would otherwise hang the page forever (the failure mode of deviation 14). The deadline bounds that. The honest consequence: on a very slow endpoint the site receives the REAL response and the user's edit does not take effect that time. §1 forbids letting that be silent, so it is not: the flag travels with the capture, and the panel surfaces it rather than leaving the user to guess. |
| 17 | M1 | **A response MockLab is only capturing (no change matches it) keeps its connection alive for up to 1.5 s — 5 s if it declares JSON — after the page itself has stopped reading.** | To list a source, MockLab reads a clone of the body. A clone's two branches share one underlying connection, so that connection cannot close until MockLab's branch is released too. The read is bounded on time and on size (2 MB) and the reader is then genuinely cancelled — measured against an endless `text/plain` response: the page let go at 116 ms, MockLab released at 1605 ms, and the socket closed. An earlier version used `clone.text()` and tried `clone.body.cancel()`, which cannot work — `text()` locks the body, so the cancel rejected out of band, the socket stayed open indefinitely (121 chunks still arriving after 12 s), and the rejection surfaced on the page as a site-visible `unhandledrejection`. MockLab now owns the reader from the first byte. |
| 18 | M2 | §9.1's two Google Fonts `@import` lines are replaced by `@font-face` rules loading Inter and Fira Code from `extension/fonts/`. | §1.4 is a non-negotiable principle: everything local, no remote servers. A font request on every panel open would send the user's IP and usage timing to a third party, in a tool whose promise is that nothing leaves their machine. Bundling delivers §9.1's typography *more* faithfully — offline, on first paint, with no fallback flash — and it removed two console errors that had been permanently excusing the panel's "no errors" check. |
| 19 | M2 | An additive `:root` block supplies tokens §9.1 does not define — chip text colours, `--on-accent`, `--chip-changed-bg`, the `--json-*` tree colours, `--scrollbar-size` and `--dir`; §9.1's own tokens are untouched. | §9.2's semantic colour pairs fail WCAG 2.2 AA at the chip's 10px/600 weight — Stale measured 2.21:1 against a 4.5:1 requirement. The additive tokens bring all four chips to 4.6–8.9:1 in both themes without altering the design system §9.1 defines. |
| 20 | M2 | The segmented control's thumb is `calc((100% - 8px) / 4)`, not §9.2's `calc(25% - 4px)`. | The spec formula lands 2px off a cell, so the thumb drifts about 6px by the fourth tab. |
| 21 | M2 | Two hex literals live in `badge.js`, outside `panel.css`'s `:root` (§17.7): the badge background and its text colour. | `chrome.action.setBadgeBackgroundColor` is drawn by Chrome and takes a literal — no stylesheet is reachable from a service worker. The values are §9.1's light-theme `--accent` and `#FFFFFF` for the text, copied verbatim. |
| 22 | M2 | Files added beyond §2.1's tree — **41 `.js` files in total** (57 in `extension/` less the 16 §2.1 names), plus `extension/package.json`, `extension/fonts/` and `extension/testlib/` (which now holds `audit.js` and `browserFixture.js`). Recorded across this row and Deviations 15, 26, 27, 33, 37 and 43. This row names: `background/{badge,changesApi}.js`, `panel/{dom,sources}.js`, `test/{changes,guards,guards.contract,guards.strings,guards.lines,panel.browser,panel.strings}.test.js`, and `extension/testlib/audit.js`. Four more exist that no row named until now — `background/pickApi.js`, `test/candidates.test.js`, `test/picker.browser.test.js`, `test/pickerdom.browser.test.js` — added at M3 and found by QA auditing §17.11 rather than by anything automated. (**This row first said "eight" and stayed there while four files were added, then said "twelve" while counting only its own list against a real total of 21** — a count in a table row no guard reads, twice.) | §17.10 caps files at ~500 lines and says to split when bigger; a single `background.js` or `panel.js` would have run past it. `test/e2e.browser.test.js` is itself 1063 lines — a browser suite is a sequence of independent scenarios rather than logic to be re-entered, and splitting it would duplicate the extension-launch fixture rather than reduce anything. `guards.test.js` in particular turns §17.4's "grep-test yourself" instruction into an automated audit that fails CI if `state: "verified"` is ever assigned outside probe.js, and `panel.strings.test.js` does the same for §17.6 — both rules PLAN.md states as something the implementer is supposed to remember, held instead by a test. |
| 23 | M2 | The badge counts enabled, non-`probe` Changes only. | Probe scaffolding exists only during a running probe, when the panel shows its own progress card. A badge flickering through a bisection would tell the user nothing true, and §17.5 guarantees probe Changes never outlive the probe. |
| 24 | M2 | `RESET_ALL` ("Reset everything", §10.5) clears Changes, Scenarios and Links on every site but not `settings`, and not the derived `signatures:` cache. It reloads only the calling tab. | `settings` holds the companion pairing token; silently unpairing the user's AI is not a data reset, and §10.5's copy gives no warning that it would. The signature cache describes what a request looks like, never what MockLab did to it, and is relearned on the next load. Other tabs are not force-reloaded because that would destroy form state on unrelated pages — they receive the emptied match list live instead, and the copy says exactly that rather than promising a refresh. |
| 25 | M2 | Strings shipped beyond §11's copy table, each marked in `strings.js` with its provenance. | §11 does not cover every surface §10 describes — the §10.2 meta row, accessible names for the per-row toggle and trash, §10.5's settings labels, editor affordances, and a `changeDropped` note for a Change that did not reach the page in time. All are written in §11's voice and routed through `strings.js`, so §17.6 holds and translation still means translating one file. |
| 26 | M3 | `extension/src/panel/pick.js` is 450 lines, a new file splitting §10.1's Pick tab out of `panel.js`. | §17.10 caps files at ~500 lines and says to split when bigger — the same reason `sources.js` and `dom.js` were split at M2. Phrased as "is N lines" so the §17.10 count guard actually reads it: the earlier wording put "a new file (" between the name and the number, which the guard's pattern skips, and the figure sat stale at 384 through two edits without anything noticing. |
| 27 | M3 | `extension/test/panel.browser.test.js` is 1077 lines. | One Chromium launch and one demo server serve both the M2 changes flow and the M3 pick flow, and the pick test depends on the clean post-reset state the M2 flow leaves behind. It passed 1000 lines with Deviation 39's two bounded-search subtests, one milestone earlier than this row predicted. **The argument first given here — that splitting buys a second browser launch and a duplicated Playwright resolver — no longer holds**: the launch and the resolver moved into a shared harness under `extension/testlib/`, so a split duplicates nothing. What remains is that the M3 pick flow depends on the clean post-reset state the M2 flow leaves behind, which is a real ordering dependency and the thing an eventual split has to solve. The split is due at M4, by flow. |
| 28 | M3 | Escape is handled in the panel as well as on the page. | §11's copy promises "(Esc to cancel)". The person's last click was the Pick button *in the panel*, so that is where focus is; the page's own keydown handler would never see the key and the promise would be false. |
| 29 | M3 | §10.1A's recent-Link selection card ships without its chevron, and the list renders nothing at all when no Link is verified — no heading, no empty box. | A chevron promises State D, which does not exist until M4. Nothing can be verified before the probe exists either, so at M3 the list is always empty; drawing a heading over an empty box would advertise a feature the build does not have (§17.12). Both return with the editor. |
| 30 | M3 | §6.1's smart-target walk accepts a parent on `area ratio ≤ 1.4` **OR** a border box within 24px on every side (text must still match; cap still 4 levels). | Measured in real Chromium: an area ratio depends on text *length* while padding does not, so the ratio is harshest on exactly the short text §6.1 names as its purpose. The demo's own pill (`padding: 5px 14px` at 12px) measures **2.71×**; a Bootstrap badge 1.86×, a Tailwind `px-2 py-0.5` 1.61×, a Material chip 2.14× (re-measured against a full 32px-tall MUI `Chip` recipe it is 3.12× — the figure is recipe-dependent, and only "> 1.4×" is load-bearing) — none pass 1.4×, and the demo's pill besides has no child element, so neither figure is even reachable on the DoD path. (**2.57× was recorded here first and is corrected** — it is a 5px/12px fixture the tests used to mislabel as the demo's own CSS; the fixture now reads the demo's real padding off disk, so the attribution is structural rather than copied.) Re-deriving 2.71× from the pill's *full* CSS gives **2.60×**, because its `font-weight: 700` and `letter-spacing` widen the text against the same padding — 2.71× is exact for the configuration named here (`padding: 5px 14px` at 12px), and this note exists so a future reader who gets 2.60 does not conclude the record drifted. No realistically padded pill passes 1.4× — the literal rule cannot do the job §6.1 describes for it. The inset half never rejects anything the ratio accepts, so nothing that worked before regresses; a same-text whole row (40×) is still blocked by both. |
| 31 | M3 | §6.3's sibling-key gate fires on *any* hit from a needle derived from the element, not only a full-text hit. | **The justification first recorded here was false and is corrected.** It claimed the strict reading makes the M3 DoD impossible. It does not: §6.3 derives the word needle `time` from "On time", and `"ON_TIME".toLowerCase()` *contains* it, so the spec's own substring rule scores `$.status` at 0.50 — rank 1, above sibling-key's 0.45. Re-measured against the real fixtures under a strict re-implementation: the DoD passes. The real reason to broaden the gate is that the strict reading makes enum discovery depend on an **accidental shared substring** between a display string and a machine constant. `"On time"`/`"ON_TIME"` happens to share one; `"Delayed"`/`"LATE"`, `"Sold out"`/`"OOS"`, or any localized UI do not. The DoD is passable either way — the product is not. Containment is unchanged and was verified both ways: a response with zero hits contributes no siblings, so nothing leaks in. |
| 32 | M3 | Candidate discovery searches to depth 24 and 20000 paths per response, 120000 per tab, and reports `searched.complete` when it stopped early. | §5.4's defaults are 12/5000. A field 13 levels down was invisible and the user was shown `pick.noCandidates` — a claim about *the data* — when the truth was that MockLab stopped looking. A bounded search reported as an empty one is a false negative told as a fact (§17.12). Raising the cap raised the worst case with it (200 sources at the ceiling blocked the service worker for 3244 ms), so the tab budget bounds it back to 163 ms; the honesty flag is what makes bounding acceptable. |
| 33 | M3 | `extension/src/content/element.js` split from `picker.js`; the manifest lists three ISOLATED content scripts. | §17.10. The seam is the one the tests already used: `element.js` answers the questions the M4 probe asks with no picker running (§6.2 fingerprint and re-resolve, §7.3 snapshot, §6.1 smart target); `picker.js` is the interaction. `extension/src/content/element.js` is 301 lines and `extension/src/content/picker.js` is 296 lines. (**Recorded first as "295 lines each", which was already false by two commits and which the line-count guard could not read** — the same blind spot Deviation 26 documents one row over, recurring in the same table. Phrased in the guarded form now, so the guard reads it; making the guard *reject* a line-count claim it cannot parse, rather than skip it silently, is tracked separately — until that lands, this table can still hide a stale number behind a phrasing.) |
| 34 | M3 | `extension/src/background/pickMessages.js` declared the pick message types instead of `messages.js`. **Resolved — the file no longer exists.** | §17.8 requires one home for message constants, and `messages.js` was owned by another agent mid-milestone. Declared once, not duplicated, with a merge recipe in its header. Folded into `messages.js` (with `PHASE`) as a no-behaviour-change commit, verified by building the merge-only tree and running it: 207/207, the exact suite and count of the M3 commit, with all seven values byte-compared against the deleted module. The merge also closed a hole nobody was aiming at — §17.2's mirror guard walks `Object.values(PORT_MSG)`, so while the pick values lived outside it, breaking `agent.js`'s mirror of `port:picked` (which kills pick mode end to end) passed all twelve guards. |
| 35 | M3 | Accent colours (`#0066FF`, `#4A90FF`, `rgba(0,102,255,.08)`, `#FFFFFF`) appear in one named block at the top of `picker.js`. | §17.7 forbids hex outside `panel.css`'s `:root`, but the overlay renders in the host page and cannot import `panel.css`. Injecting it would need `web_accessible_resources` (exposing the stylesheet to every page) *and* would drop the panel's `:root` variables onto the host document, changing the site's own colours — breaking §17.2, which outranks §17.7's mechanism. Same precedent as Deviation 21 (`badge.js`). All four values are stated verbatim in §6.1. |
| 36 | M3 | The hover overlay renders inside an **open shadow root**. Additive to §6.1. | Mutation-proved: on a page with `* { border: 0 !important }` the site's own CSS erases MockLab's outline and the user sees nothing at all while believing they are picking. |
| 37 | M3 | `candidates.js` matches against a pre-enumerated leaf list instead of calling `findByValue` once per (source × needle). | The literal reading is 200 sources × 12 needles × a 5000-path walk. Semantics are held identical by a differential test over 6 bodies × 19 needles, not by a comment. |
| 38 | M3 | Two candidate scores §6.3 does not price: `attrExact` 0.95, `wordExact` 0.6. | §6.3 prices *how* a leaf matched, not *what* matched. Scored at 1.0 the word "Total" would outrank the number the user actually pointed at. |
| 39 | M3 | Two strings beyond §11's copy table, `pick.searchIncomplete` and `pick.listIncomplete`, and the Pick tab renders them instead of `pick.noCandidates` whenever Deviation 32's `searched.complete` is false. The empty-and-bounded screen also drops §6.3's "Check all fields" button. | §11 phrases `noCandidates` as a claim about the **data** — "couldn't find this text in any data the page loaded" — and a bounded search never established that; §17.12 makes a confident wrong answer the worst thing this product can do, and this is that failure in its honest-sounding direction. A non-empty bounded list gets the second string for the same reason: twelve rows in likelihood order read as "these are the possibilities", which is a completeness nobody proved. "Check all fields" is not offered as the way out because it cannot run before M4 **and** an exhaustive pass is the same enumeration under the same ceilings — a grey button under that sentence would read as a cure that is neither available nor certain. Both strings name the Sources tab instead, whose tree really does render whole response bodies to any depth. |
| 40 | M3 | Playwright is **not** a declared dependency of either workspace; the browser suites resolve whatever copy is present and skip when there is none. | The suites are written to skip rather than fail so `npm test` works on a machine without a Chromium build (§2.1 asks for a dependency-free extension, and Playwright is a test-only tool). The cost is that an ordinary dependency change — an `npm ci` that wipes an undeclared install — silently removes all browser coverage while CI stays green. Deviation 41 is what makes that cost survivable. |
| 41 | M3 | The CI browser job asserts each suite reported **≥ 1 pass and exactly 0 skips**, not merely that `node --test` exited 0. | Measured, not assumed: with a Chromium build unavailable, `node --test` exits **0** while reporting `# pass 0 # skipped 1`. A job that only checks the exit code is green having proved nothing. This repository has already shipped a browser suite CI never invoked (M2 B1); invoking it is necessary but not sufficient — the run has to be shown to have happened. Verified in both directions against a real skip, not a simulated one. |
| 42 | M3 | `fields` in §10.2's meta row (and §12.4 #2) is an exact leaf count from a new `jsonpath.countLeaves`, not §5.4's bounded `enumeratePaths`. §5.4 lists four functions; this is a fifth. | A count is a claim about **the data**, while a search ceiling is a fact about **the search** — so neither 12 nor 24 is the honest number, they only move the boundary (at depth 24 the fixture reports 6023 of 6031 fields). Unlike discovery, counting is O(nodes) over a body §4 already caps at 2 MB, so the bound can simply be removed with nothing left to truncate or disclose. Measured: a 50-request page load got *faster*, 3.9 → 1.5 ms while counting a third more fields (no path strings built); worst single 2 MB body 40.6 → 42.9 ms. Iterative on purpose — `JSON.parse` accepts nesting past any recursion limit, and a swallowed `RangeError` would report "0 fields", the same lie louder. |
| 43 | M3 | The guards are split by theme — `extension/test/guards.test.js` is 464 lines, `guards.contract.test.js` is 772 lines, `guards.strings.test.js` is 495 lines and `guards.lines.test.js` is 553 lines, plus shared helpers: `extension/testlib/audit.js` is 171 lines. `messages.js` gained a fourth exported contract, `CONTENT_GLOBALS`. | **Kept because the reasoning that nearly prevented the split is worth more than the split.** (This row said "no longer a deviation — every file is inside §17.10's budget" and that stopped being true as the guards grew: `guards.lines.test.js` is 553. The count guard checks *numbers*, not *sentences*, so nothing could see it — the eighth figure-shaped claim on this build to sit outside coverage, and the first that was prose rather than a number.) It grew 520 → 808 → 1031 across two commits, both of them commits fixing record defects, and was twice recorded rather than split for the same reason the two browser suites are (Deviations 22, 27): each regex here is only safe to read next to the boundary note that qualifies it, and a split separates them. The alternative costed out was a second test file plus a shared `extension/testlib/` helper, which would need adding to the audit's own file list or it escapes the §17.10 check silently — trading a recorded overage for an unrecorded blind spot. **This row was accepted at 808 lines on the argument below and has since grown to 1031, which is where that argument stops being free.** It stays recorded for now because the split must not land in the same change that closes M3 — these are the guards M4's single `state: "verified"` assignment is checked by, and rearranging them while building the thing they check is how a guard gets quietly weakened. **The first version of this row argued for deferring it and got that wrong.** It argued you must not rearrange a guard while building the thing it checks, then concluded "split at M4" — which is exactly that. The principle rules out the M3-closing commit *and* the M4 build; the only safe slot is between them, which the row failed to consider, licensing the more dangerous of the two options it set out to avoid. QA caught it. The split is by theme (ISOLATED-world contract, §17.6 string audit, line-count audit), held to the bar `9daef6c` already set for a no-behaviour-change commit in this repo: identical test-name set and count either side, plus mutations that must fail the same subtests on both. The "a shared helper directory would escape the §17.10 audit" objection was cited as a blocker and turned out to be one line in `ALL_FILES` — the deviation doing a decision's work. It was closed better than proposed: the roots are now derived from `package.json`'s workspaces rather than hand-listed, which removes the class of mistake instead of one instance, and is mutation-proved — a 602-line helper and a stray `__mocklabSecretHandshake` global dropped into `testlib/` were **silent** before and are caught now. The split itself was held to the `9daef6c` bar: the sorted test-name set is byte-identical either side at 231 names, and three mutations fail the same subtests on both. The growth bought two things a smaller guard would not have: the ISOLATED-world globals (`__mocklabElement`, `__mocklabPicker`, `__mocklab_overlay__`, `__mocklabInterceptorInstalled`) are now mirrored from one home like the message constants, and the method audit resolves receivers rather than matching `__mocklabElement.NAME(` — a literal reading of which finds **zero** call sites in `src/` and would have reported green while checking nothing. |
| 44 | M3 | The line-count audit treats a bare number appositive to a backticked `*.js` path as a claim about that file, not only one carrying the word "lines". | The first hardening keyed on the word, so a claim omitting it was invisible to the reader *and* the checker: QA rewrote this table's own guard counts to `999/888/777/666` and the full suite stayed green. Two boundaries are recorded in the test rather than decided silently — apposition is read in one direction only (Deviation 35's `` Deviation 21 (`badge.js`) `` is a cross-reference in the reverse order, and is a live fixture so whoever closes that side finds the blocker on screen), and the number must end on punctuation so `` `x.js` 3 times `` stays a quantity of something else. **Six figures have rotted on this build and every one sat outside guard coverage; not one in the guarded form ever has.** A repo-wide extension was measured and rejected — roughly nine in ten matches are honest prose, and many are unfixable by rewriting (an aggregate over two files, a count of tests rather than lines, a deliberate quotation of a superseded figure), which makes it the kind of guard people route around. The end of this class is generated counts — a script that rewrites the figures in a marked block, with the audit checking only that the block is fresh — not a better regex over hand-typed ones. |
| 45 | M3 | A browser suite whose fixture dies now reports **every** check it did not run — failed if the fixture broke after the browser launched, skipped if before — so its contribution to `# tests` is constant. All four suites carry the full form, via `extension/testlib/browserFixture.js`. | A real run showed `# tests 211 # pass 210 # fail 1`: an outer fixture died and took 22 subtests with it, and CI would have displayed a green-looking "210 pass". It was identified only by arithmetic against a total someone happened to know. Stage budgets are measured, not guessed — launch 198/208/223 ms idle and 282/345/458 ms under load, service-worker registration 0/0/3 ms, so a 20 s wait has enormous headroom **when the suites run one at a time** — which is how `.github/workflows/ci.yml` runs them. **That is not the whole picture, and the figure was first recorded here as an unqualified "~1000× headroom".** `npm test -ws` lets `node --test` run files concurrently, so on a 4-CPU box four Chromiums launch at once; on a cold binary cache one observed run took 1286 ms to launch and then exceeded the **20 000 ms** service-worker budget outright, failing all 8 checks of one suite. Raising the budget is still the wrong response — the contention is the cause — but anyone reading this row should know the sequential measurement does not describe the command the DoD tells them to run. Honest limit: the budget makes a hung stage legible, not fast — an abandoned stage still holds the event loop until Playwright's own promise settles. This row first recorded `pickerdom` as carrying only the named-stage half because §17.10's cap left it 8 lines; the shared harness removed that constraint — `picker` went 517 → 419 lines and `pickerdom` 519 → 482, both now carrying the full form. |
| 46 | M3 | `extension/src/content/element.js`'s prose is length-frozen for anyone who cannot also edit `README.md`. | Deviation 33 records its exact line count, and the §17.10 audit enforces it — so an agent that owns the source but not the record cannot add or remove a comment line without failing the build. Hit for real this milestone: a stale-claim fix had to be rewritten line-neutrally. This is a genuine cost of recording exact counts by hand, and one more reason the end-state is generated figures (Deviation 44). |
| 47 | M3 | `extension/testlib/browserFixture.js` is 478 lines and owns the Chromium launch, the Playwright resolver, a staged fixture, and (since M7 — Deviation 90) the service-worker error recorder, for all eleven browser suites. Only a stage declared `{ absent: … }` may turn a failure into a skip; every other stage fails by name. | Built after a real hang was found by injection: `boot()` returned its context only on success, so a launch that succeeded followed by a service-worker wait that failed left a whole Chromium with no reference on it and `node --test` waited on that process forever — plus a demo-server and profile-dir leak sitting outside any `try`. The classification seam exists because `e2e`'s catch reported a 20 s service-worker timeout as "Chromium could not be launched": a genuine failure wearing an environment gap's clothes, which CI would have caught (Deviation 41) with the wrong diagnosis. `panel`'s fixture also dropped `networkidle` — the only such wait in any fixture, on Playwright's default 30 s timeout — for the condition it actually needed (both demo sources rendered), and 1400 ms of fixed sleeps for a measured 128–144 ms wait. Name-set identical either side at 242 names. **Three `networkidle` waits remain inside `panel.browser.test.js` subtests, deliberately untouched** — changing the 10-refresh loop risks changing what it asserts, and each carries the same hang risk one subtest at a time. |

| 48 | M4 | Three files added beyond §2.1's tree, all §10.1: `extension/src/panel/probe.js` is 391 lines (the probe's message contract, its progress card and its failure cards), `extension/src/panel/result.js` is 285 lines (State D — the result card and its value editor), and `extension/testlib/verifiedChip.js` is 98 lines. `panel.js` would have run past §17.10's cap with any of them in it, and `pick.js` is 450 lines (also recorded at Deviation 26 — **two rows carry this file's count, so a change must update both or the guard fails on whichever is stale**; a figure belongs in exactly one row) before State D is added to it. | §17.10 caps files at ~500 lines and says to split when bigger — the same reason `sources.js`, `dom.js` and `pick.js` were split before it. The seam between `probe.js` and `result.js` is the experiment against its outcome, and it is one-way: `result.js` imports `proved()` and `linkChip()` from `probe.js`, never the reverse, so the question "is this link verified?" is asked in exactly one place. `verifiedChip.js` holds §17.12's invariant so that the two suites which render the panel cannot drift apart about what honesty means there — see Deviation 50. |
| 49 | M4 | `extension/test/panel.probe.browser.test.js` is 838 lines. | This is the split Deviation 27 called due at M4, done by flow rather than by size: `panel.browser.test.js` is the M2 changes flow followed by the M3 pick flow, and the second depends on the clean post-reset state the first leaves behind. §10.1's probe screens depend on neither, so they are a new file that shares `testlib/browserFixture.js` and duplicates nothing — and none of the three `networkidle` waits Deviation 47 leaves inside that suite is disturbed. Over the budget for the reason Deviations 22 and 27 give: a browser suite is a sequence of independent scenarios sharing one Chromium launch, not logic to be re-entered. |
| 50 | M4 | The guard that kept §11's "Verified ✓" out of the panel body **changed shape instead of being deleted**: the chip may now appear, and only on a Link whose own state is `verified`. Three independently written facts must agree for every node that reads the word — `S.chips[state]` for the word, `chip--<state>` for the colour, and `data-link-state` on both the chip and the card around it — plus a count the fixture supplies. | From M2 to M3 the suite asserted the word appeared **nowhere**, which was true by construction while no probe existed and was the single assertion standing between this product and §17.12's worst bug. At M4 the word becomes legitimate and that assertion could only have been deleted — which is how a build ships the bug it spent three milestones avoiding. Proved by mutation, not by passing: widening §10.1A's filter from `=== 'verified'` to `!== 'stale'` fails the count in both suites, printing the chip for a `candidate` link fails the word/colour agreement, and rendering `probe.found` for an unproved link fails State D's own check. |
| 51 | M4 | The label on a filled button is `--on-accent-text`, which is white in the light theme and `#1E1F24` in the dark one, rather than white in both. | §9.1's dark `--accent` (#4A90FF) is a light blue: white 14px/600 on it measures **3.12:1**, under WCAG 2.2 AA 1.4.3, and that is the label on "Apply & refresh page" — the button this whole product exists to have someone press. Dark `--danger` (#F28B82) under the danger toast was worse at 2.25:1. Inking the label rather than inventing a second blue keeps one accent colour in the UI and follows the same rule the light theme already follows: a light fill takes dark text. Measured after: light 4.83 on `--accent` and 4.77 on `--danger`; dark 5.27 and 6.89. §9.1's own token block is untouched; the change is in the additive extension-token block below it, beside the four chip text colours M3 added for the same reason. `panel.probe.browser.test.js` now measures every filled button in both themes, which nothing did before. |
| 52 | M4 | The `.spinner` is a stroked ring whose radial gradient is centred on the TOP of its box, and it paints in `currentColor`. | §9.2 asks for the DGA radial-gradient spinner. The version shipped from M2 was a disc filled with a gradient centred **on itself** — radially symmetric, so `animation: spin 1s linear infinite` painted an identical frame at every angle and the loader never appeared to move — with a smaller disc of `currentColor` laid over the middle as a fake hole, which drew a solid dark dot in a §10.2 tree row and a white blob inside a primary button. Nothing tested it, and it is the hero element of the one screen §10.1 says must "NEVER let the user think it's stuck". It is now asserted by screenshotting the element at two animation times and requiring the pixels to differ. |
| 53 | M4 | A tooltip may be anchored to its control's inline END (`withTip(…, { end: true })`) instead of centred on it. | §9.2's bubble is up to 14rem wide and centred; on a 320–420px panel a control near the edge pushes it off the panel entirely — measured at 42px outside on §10.1D's "Show me". Anchoring to the control's own end is the only placement that cannot overflow on that side. The suite now asserts at 320, 360 and 420px that no tooltip on these screens is rendered outside the panel box. |
| 54 | M4 | §7.1's CONTROL_A is specified as *"reload with zero mock Changes"*; the probe instead leaves any Change the user already made in place and appends its own. | Reverting a user's Change to take a control reading means the page the probe measures is not the page the person is looking at, and restoring it afterwards is a write MockLab would have to get exactly right on a cancel or a crash. Verified in Chromium: with a user Change already on `$.status`, the probe's appended Change wins, verification completes in 6 reloads with both elements found, and the user's Change survives. **`probe.js` said "Recorded in README" while this row did not exist** — found by QA reading the source against the table, which nothing automates. |
| 55 | M4 | §7.2 says the noise mask covers *visible* ancestors and siblings; `agent.js` filters none out and masks them all. | A hidden node that changes between control runs is still noise, and `visible` is not cheaply decidable without forcing layout on every candidate. Masking more can only cause a refusal, never a false confirmation — the safe direction under §17.12. **Also carried "Recorded in README" in source while unrecorded.** |
| 56 | M4 | A probe can return a false `verified` for an element driven only by load index, when its period aligns with the probe's schedule (X,X,Y,Y,Y,X). | Two control runs cannot detect period-≥3 noise; this is a limit of §7.1's protocol as specified, not a departure from it. §7.1's own paranoid cycle is the answer and works — it kills the variant that survives cycle 1 but fails cycle 2. Recorded because §17.12 makes a wrong "Verified ✓" the worst outcome this product can produce, so the one shape that can still cause it should be written down rather than left for someone to rediscover. |
| 57 | M4 | `extension/src/background/messages.js` is 905 lines. | It is the single home §17.8 requires for every message constant, and every milestone adds to it — `MSG`, `PORT_MSG`, `PAGE`, `PHASE`, `CONTENT_GLOBALS`, and now `PROBE_MSG`/`PROBE_PHASE`/`PROBE_STEP`/`PROBE_FAIL` folded in from M4's staging file. Splitting it would recreate exactly the hole the fold-in closed: while the probe port literals lived outside `Object.values(PORT_MSG)`, breaking `agent.js`'s mirror of `port:probeSnapshot` left all 248 unit tests green while the probe died on reload 1. One file is what makes the mirror guard total. |
| 58 | M4 | `extension/src/panel/panel.css` is 695 lines, and the line-count audit now reads recorded counts for non-`.js` source files. | §17.10 caps files at ~500 lines and says nothing about the extension, but the audit only parsed backticked `*.js` paths — so a `.css` row would either go unchecked or fail the build, and panel-designer correctly declined to force one. That made a 542-line file over budget and **structurally invisible**: the seventh figure on this build to sit where the audit cannot look, and the first where the file itself was the thing hiding. The stylesheet carries §9.1's token block verbatim, §9.2's component recipes, and every screen M2 through M5 added; splitting it separates a token from the rule that uses it, which is the drift §17.10 exists to prevent. |
| 59 | M4 | `extension/src/background/probe.js` is 511 lines. | §7's state machine is one protocol: CONTROL_A/B, the noise mask, bisection, VERIFY_ON, VERIFY_OFF, CLEANUP and the paranoid cycle read as a sequence, and each state's guard clause is only correct in the context of the one before it. The parts that could be lifted out already were — `probeLink.js`, `probeValues.js`, `probeQueue.js`, `probeChanges.js` and `diff.js` are separate files. What is left is the transition table itself, which is where §17.12's whole claim is decided: the single `state: 'verified'` assignment sits at the end of it, reached only from CONFIRMED. |
| 60 | M5 | `extension/test/candidates.test.js` is 533 lines. | §6.3's scoring is one table of rules that only make sense against each other — a substring hit outranking a sibling-key hit is the M3 DoD, and both are read against the same fixtures. Splitting it separates a score from the case that justifies it. |
| 61 | M5 | Seven files added beyond §2.1's tree: `panel/{links,scenarios}.js`, `shared/scenarioFile.js` (born in `panel/`, moved at M7 — see Deviation 84) and `test/{scenarioFile,panel.links,panel.scenarios.browser,panel.copy}.test.js`; `panel.copy.test.js` split from `panel.strings.test.js` (which had reached 540). | §17.10. `scenarioFile.js` is pure, so the hostile-input matrix runs without a browser; `links.js` holds §1.1's third state and is read by three screens that may not import each other. The test split's seam: *does the panel hold words of its own* (strings) versus *does the copy table have the properties the screens need* (copy). Both are under budget now. |
| 62 | M5 | `--on-danger-surface` (`#CC2D23`) added to the additive token block. | §9.1's `--danger` on `--bg-danger` measures **4.17:1** in light — under AA for the 12px text that sits there: the friendly import error the M5 DoD asks for, §10.5's danger-zone heading, and every `.btn--danger` on hover. Same hue within 3.7°, same saturation; 4.63 on `--bg-danger`, 4.90 on `--bg-secondary`, 5.29 on `--bg`. Dark was already 5.99 and is untouched, as is §9.1 itself. |
| 63 | M5 | Import **refuses** a scenario saved on another site, naming that site, rather than importing it. | Its Changes are addressed by a source identity derived from the site's address, so here they could never match. Importing would produce a card that is Stale from birth, under a sentence — "this site seems to have changed" — that would not be true of anything. |
| 64 | M5 | §10.4's "grid" resolves to a single column at 320–420px (`auto-fill, minmax(13rem, 1fr)`), becoming a real grid only when the panel is dragged wider. | A card carries emoji, name, status chip, ⋯ and Apply; in the 158px columns a two-up grid produces at 360px, the chip wraps under the name. |
| 65 | M5 | `stale` is computed by the panel at draw time, never stored. The store holds only `verified` and `candidate`. | §1.1's third state becomes true while nothing is running — a source stops appearing, or a highlight draws nothing — so there is no moment at which a writer could record it. `links.js`'s `shownLinkState()` is downgrade-only: it compares against `'verified'` and otherwise returns the stored state untouched, so it structurally cannot *produce* the word (§17.4). |
| 66 | M5 | The probe's unit suite passed the budget and was **split** rather than recorded: `extension/test/probe.test.js` is 224 lines and `extension/test/probe.failures.test.js` is 356 lines. | This row first argued for recording the overage, on the grounds that splitting §7's mutation matrix separates a mutation from the rule it disproves. probe-engineer split it anyway, along a seam that keeps that property: the protocol's happy path and its refusals are different claims, and each file still holds its mutations beside the states they attack. Both are under budget, so there is no deviation left — the row is kept because the argument for recording it was wrong and the split is the better answer. |
| 67 | M6 | `extension/src/background/background.js` is 688 lines, past §17.10's budget. | The M6 wiring is what pushed it over: `routeMessage` lifted out of the `onMessage` listener, and the client that opens the socket. Neither can move. A service worker cannot `chrome.runtime.sendMessage` to itself, so the MCP bridge reaches the panel's handlers only by being handed the worker's own router — and it must be that one, or an agent's `set_value` and a person's "Apply & refresh page" would be two implementations free to disagree (§1.6). Splitting the router into a module of its own would take ten constructed objects as arguments and leave this file — whose whole subject is wiring — holding a call that reassembles them. The parts that could leave already have: `changesApi`, `pickApi`, `probeApi`, `presets`, `highlight`, `badge` and `wsOps` are separate files, and M1's capture pipeline is the only large block left, which belongs to another owner. |
| 68 | M6 | `extension/test/mcp.browser.test.js` is 682 lines and may not be split; `companion/test/hub.test.js` was **split** instead, giving one file beyond §2.1's tree: `companion/test/demo.test.js` is 54 lines. | The browser suite binds **127.0.0.1:8517** — §12.1's real port, because a shipped extension has no configuration and its client dials that and nothing else. `node --test` runs test FILES in parallel, so a second suite needing the same port would fight this one for it and the loser would skip; the eleven checks are one sequence sharing one Chromium, one pairing and one demo page (Deviations 22, 27, 49 record the same shape). `hub.test.js` had a real seam and its own header already named it — §14's demo site serves static files and opens no socket — so that one was split rather than recorded: `companion/test/hub.test.js` is now 520 lines, inside the budget, and the demo file carries §14's three checks. |
| 69 | M6 | Three OPTIONAL arguments added to §12.4's schemas: `set_value.enabled`, `probe_element.exhaustive`, `probe_element.paranoid`. `probe_element.paranoid` writes the SETTING §10.5 puts in a checkbox rather than being a per-call flag. Two further gaps are left **open**: renaming or importing a Scenario, and "Reset everything". | Each argument closed a case where the panel offered a person something an agent could not reach at all (§1.6) — switching a Change off without deleting it, §10.1D's "Check all fields (slower)" offered precisely after an honest `noneConfirmed`, and the extra-careful checking that changes how `probe_element` itself behaves, so an agent that could not set it could not reproduce a person's probe. `additionalProperties:false` means these were REFUSED before, not ignored. Adding an optional argument is not the act renaming or reordering one would be: no existing call changes meaning and the fifteen names and their order are untouched. `paranoid` is written to the store because a private per-call copy would be a second answer to "how careful is MockLab being?", which the panel and the agent could then each read differently. The two remaining gaps would each need a SIXTEENTH TOOL, which changes the surface §12.4 names, so they are recorded and not built. |
| 70 | M6 | MCP's own cancellation is honoured: `extra.signal` -> a hub `cancel` EVENT frame carrying the request id -> the extension's `CANCEL_PROBE`. Not in §12.2's frame table. | §7.1 says the user can cancel any time and CLEANUP puts the page back. An MCP client that abandons a `probe_element` is doing exactly that, and without this the browser never hears about it: the page a person is looking at goes on reloading for up to three minutes for a caller that stopped listening. MCP already has cancellation, so a sixteenth tool would have been a second vocabulary for one idea. It is an `event` and not a `req` because a `req` reusing that id would be answered with a `res` carrying the same id — the frame the hub reads as "that call is finished". |
| 71 | M6 | The extension's socket dials **only once the browser has been paired**, and dials the moment a token lands in `chrome.storage.local`. §12.3 describes the token on every connection but does not say when the first one happens. | An unpaired MockLab has no token, and the hub refuses a tokenless socket unless a pairing window is open — so a client that dialled anyway would retry a port nobody is listening on every 25 seconds for the whole of a browsing session, writing a connection error into the user's console each time for a feature they never turned on (and into the service-worker console the e2e suite asserts is clean). Connecting on the storage write is the other half: pairing is finished in one extension context and the socket is owned by another, so without it a user who typed the six digits correctly waits out a backoff of up to 30 seconds in front of a Settings screen still reading "Not connected". |

| 72 | M7 | Deep mode's CDP Fetch patterns are **Document-only**, though §8 lists Document, XHR and Fetch and says the interceptor is told to capture-only. | Narrowing to documents removes the double-application at its root instead of managing it: §5.1's MAIN-world patch keeps XHR and fetch, so turning deep mode on changes nothing else and §7's probe still works. Muting the interceptor instead would mean any detach leaves a site where the setting says on, nothing is intercepting, and **no Change applies at all, silently** — the worst of the three states. One paused navigation per load is also a far smaller blast radius than every request. Cost: a response the MAIN world cannot rewrite (a consumed stream, §5.1.4) stays un-rewritable. |
| 73 | M7 | Deep mode never applies `probe:true` Changes, so a probe over a document-embedded field ends in `probe.noneConfirmed`. | Document fields are editable but not provable, and `noneConfirmed`'s words are exactly true of one. **Deep mode therefore adds no second road to `state: 'verified'`** — a field this engine does not mutate cannot move an element, so it can only be discarded or fail VERIFY_ON. That was the design constraint, not a side effect: §17.12 makes a second path to that assignment the most dangerous thing this milestone could have added. |
| 74 | M7 | Interception starts at a tab's **second** load, and an unexpected detach clears that origin from `deepModeOrigins`. | Measured rather than assumed: `tabs.onUpdated` delivers `changeInfo.url` at navigation *commit*, after the response, and the attach itself takes ~6 ms — no trigger under §3's permissions fires earlier. The self-clearing matters more: a checkbox saying deep mode is on while nothing is intercepting is a false statement about MockLab's own state. The honest gap is that the tick vanishes with **no sentence beside it**, because `deep.devtoolsConflict` has no surface and adding one needs a message type. |
| 75 | M7 | Document sources are deliberately kept out of `signatures:<origin>`, and a document sigId is `__document__:<sig12>:<blockKey>` rather than §8's bare `__document__`. | Keeping them out of the match list means a document Change can never enter the MAIN-world matcher, where it could only ever apply to the wrong thing. The block key is in the id because one document may carry several embedded blocks, and merging them would put `__NEXT_DATA__` in front of a person who is never shown jargon (§11). |
| 76 | M7 | `extension/src/background/background.js` is 688 lines. | Deviation 67's argument stands — the MCP bridge must be handed *this* worker's router — and deep mode added only ~90 lines. There **is** now a real seam: lifting the per-tab capture store out would leave roughly two fifths of this file behind, all of it pure wiring. It was not taken in the last milestone with two agents live in the tree, because it rewrites M1's block wholesale. Scheduled as its own no-behaviour-change commit, held to the bar `9daef6c` set. |
| 77 | M7 | **The dark theme paints its filled buttons DARK-on-accent, and the §9.2 hover sheen is white at 14% rather than 22%.** §9.2 says "accent bg, white text … + white 135° gradient sheen". | Two measurements, and the second is not a dark-mode problem at all. (a) §9.1's dark `--accent` (#4A90FF) is a light blue: white 14px/600 on it is **3.12:1**, under WCAG 2.2 AA 1.4.3, and no text colour reaches 4.5:1 on it — so the label has to be inked, not re-shaded. Dark `--danger` under the danger toast was worse at 2.25:1. After: light 4.83 / 4.77, dark 5.27 / 6.89. (b) The sheen was then measured for the first time, HOVERED, and white at 22% over the LIGHT theme's `--accent-hover` (#0052CC) puts the white label at **4.34:1** — a failing state on "Apply & refresh page" in the theme §9.2 was drawn for, invisible because only the resting button had ever been measured. White is kept (a sheen inked to match the label costs contrast in whichever theme uses it — dark ink over #5EA0FF measures 4.32:1 and was tried first); the strength drops to 14%, where the same worst pixel measures 5.14. The checkmark on a checked checkbox follows the label for the same reason. `panel.probe.browser.test.js` now measures every filled button at rest AND hovered, in both themes, taking the gradient's first stop rather than an average. |
| 78 | M7 | **`panel.html` states neither `lang` nor `dir`; `panel.js` applies both from `S.meta` at boot.** | §9.2 promises "RTL-ready … all strings routed through `strings.js` so Arabic can be added by translating one file". Every layout rule in `panel.css` was already written in logical properties and read its handedness from `--dir` — and `panel.html` line 2 carried `dir="ltr"`, so translating the one file produced an Arabic panel laid out left-to-right. Direction is a property of the COPY, so it lives with the copy. Nothing is lost before the script runs: every word on the page is written by `panel.js`, so a panel without it is blank rather than mis-labelled. |
| 79 | M7 | **A control handed to `withTip` `disabled` is converted to `aria-disabled`**, and `panel.css` reads both in every "is this usable?" selector. | `disabled` removes an element from the focus order AND stops it dispatching pointer events, so `.tip:focus-within` could never fire and the wrapper needed `pointer-events:none` on the child to get the hover at all. Three controls — Deep mode, "Set up AI access", "Show on page" — therefore explained why they did nothing **to a mouse and to nothing else**, for six milestones. Two now carry visible help text instead (§10.5 has room for a sentence); the third is a per-row icon button in a dense tree with nowhere to put one, so the tooltip itself was made to work: focusable, `aria-describedby` to the bubble, and activation swallowed on the wrapper in the capture phase — before any handler the caller attached, so an inert control is inert whatever its own listeners think. |
| 80 | M7 | **WCAG 2.2 1.4.13 for every `.tip`:** a shown bubble is hoverable, and Escape dismisses every bubble until the pointer or the focus next moves (`wireTips` in `dom.js`, a `tips-hushed` class on `<body>`). | The bubble was `pointer-events:none` at all times, so moving the pointer onto it dropped `.tip:hover` and the text vanished out from under the pointer — the failure 1.4.13 names, worst on the longest bubbles. And there was no way to dismiss one without moving away from the control, though a 14rem bubble covers whatever sits under it (on the tab strip, "Reset site"). The hush is a body class and not per-node state because the panel rebuilds its DOM on every store update — a flag on a node would be dropped by the next render while the pointer had not moved, i.e. a dismissal that undismisses itself. It lifts on a pointer MOVE of more than 4px and not on `pointerover`: hiding a bubble the pointer rests on changes the element under it, and lifting there put the bubble straight back (measured, not reasoned). |
| 81 | M7 | **The §10.2 source card's checkbox is a real, focusable checkbox with `aria-expanded`**, ringed on the card. It carried `tabindex="-1" aria-hidden="true"`. | §9.2 builds the selection card's "selected" look on `:has(input:checked)`, so the input has to stay — but the `<label>` around it is not focusable either, so the control that opens a source was reachable by mouse and by nothing else. Every source in the panel, on the tab §10.2 is about. `aria-expanded` because a response tree is being opened, not a selection made (ARIA 1.2 allows it on the checkbox role), and the focus ring goes on the CARD because the input is invisible and has no area to ring. |
| 82 | M7 | **`render()` restores focus by `id` OR by a `data-focus` key.** | It understood `id` alone, and only the panel's text inputs and the value picker's radios have one — so every other control dropped the focus on the floor the moment it did anything. Operating the §10.2 tree from a keyboard meant: Tab to a source, press Space, and land back on `<body>` with the tree open. §16 M7 asks that every control be keyboard-reachable, and one you can reach once per press is not. `data-focus` rather than an id because an id is a document-wide anchor and a tree row's key has to carry a source and a field, which contain characters that make a poor one. |
| 83 | M7 | **The three §10.2 tree-row tooltips are anchored with `.tip--end`.** | Measured at the panel's own design width, a 14rem bubble centred on a 28px icon hard against the inline end hangs **32px off the panel** — in the shipped LTR build, since M2. `panel.css` has carried `.tip--end` for exactly this since §10.1D's "Show me"; these rows never got it, and every tooltip-overflow check that existed was on a different screen. Found by the new RTL sweep, which is the first thing to measure every tooltip in the panel at once. |
| 84 | M7 | **`panel/scenarioFile.js` moved to `src/shared/scenarioFile.js`,** and its path rule now asks `parsePath` (§5.4's own parser) instead of mirroring it in a regex. | It stopped being panel-only at M6: `background/presets.js` validates `IMPORT_PRESET` payloads through it, so the socket and the file chooser are checked by one validator rather than two written to the same rules. `shared/` is where a module a worker and a panel both import belongs, beside `jsonpath.js` and `diff.js`. The mirrored regex was also **narrower than the grammar** — double-quoted bracket keys only, while `parsePath` takes single quotes too — and that was reachable: `set_value` over MCP accepts `$['a b']`, the Change is stored, `save_preset` snapshots it, Export writes it verbatim, and MockLab then refused its own file as "not a MockLab scenario". |
| 85 | M7 | **`panel/links.js` maps a NAMED refusal from `HIGHLIGHT` to `errors.pageBroke` and no longer latches the control off.** Only a worker that answers nothing at all still means "not ready yet". | The old mapping was right while nothing answered `HIGHLIGHT`. Since `background/highlight.js` landed it refuses by name — `no-tab`, `no-content-script` for a `chrome://` page or a tab opened before MockLab was installed — and every one of those told the person a built feature was unbuilt AND disabled "Show on page" for the rest of the session, on every site, until the panel was reopened. One honest sentence became a wrong one and a dead button because a change three files away made it stop being true. |
| 86 | M7 | **The Settings tab is its own file:** `extension/src/panel/settings.js` is 176 lines, and `extension/src/panel/panel.js` is 402 lines again. The danger zone's "Reset this site" gained a written reason when it is switched off (`S.settings.nothingToReset`). | §17.10, along the seam every other tab already uses: one tab, one screen, one file. The fourth inert control on that screen was grey and silent — no tooltip, not even a bad one — which is §1.1's "visibly present and silently does nothing" with nothing at all to read. It states the world rather than a next step, which is the one case §11's "always say what to do next" has no answer for; its neighbour `settings.resetAllNothing` already reads that way for the same reason. |
| 87 | M7 | **A tenth browser suite: `extension/test/panel.a11y.browser.test.js` is 692 lines,** added to CI's `for suite in …` loop in the same change. | §16 M7's DoD is three properties of the WHOLE panel — every control keyboard-reachable, reduced motion, an RTL smoke test — and each of the four existing panel suites drives one flow. Split across them, "every control keyboard-reachable" would have meant "the controls that flow happens to render". Over the budget for the reason Deviations 22, 27, 49 and 68 give: a browser suite is a sequence of independent scenarios sharing one Chromium launch. It found three defects nothing else had (Deviations 76, 77, 78) and it derives its subjects from the DOM rather than naming today's controls, so the next control added inherits the checks. Its motion-freezing helper is not a convenience: the panel is a BACKGROUND tab in this fixture (the demo has to be in front or the panel describes itself), a background tab's animation timeline does not tick, and a transitioned property therefore reads its START value forever — `[dir=rtl]` reported the thumb at +256.5px with `--dir` already at -1. |
| 88 | M7 | `extension/src/panel/sources.js` is 516 lines. | §17.10, and it is at the ceiling rather than comfortably under it. The remaining seam is real — `formatValue`, `draftFor`, `valueKind` and `fieldLabel` are value formatting that `pick.js` and `result.js` also import — but moving them touches three other owners' import lines, so it is recorded here for the next change rather than taken in the milestone that is also rewriting this file's tooltips and focus keys. |
| 89 | M7 | `extension/test/panel.scenarios.browser.test.js` is 546 lines. | It crossed the budget in this milestone, by the assertions that hold §10.4's two disabled controls to a reason a keyboard can reach — `aria-disabled`, focusability, and `aria-describedby` resolving to the sentence itself. Over the budget for the reason Deviations 22, 27, 49, 68 and 87 give: a browser suite is a sequence of independent scenarios sharing one Chromium launch and one demo server, not logic to be re-entered, and splitting it duplicates the fixture rather than reducing anything. |
| 90 | M7 | **`worker.on('console')` records NOTHING for an extension service worker in this Chromium, so five suites' closing check could not fail.** `recordWorkerErrors()` in `extension/testlib/browserFixture.js` wraps `console.error` inside the worker instead, adds `unhandledrejection` and `error` listeners, and FAILS when the wrapper is absent rather than reading an empty list as a clean one. `e2e`, `probe`, `highlight`, `picker` and `deep` now use it; `mcp` makes the claim for the first time. | Measured twice, once at M7 and again before this code was written: a `console.error` called straight out of `sw.evaluate` produced an empty event list, and so did `console.log`, `console.warn`, a `worker.on('pageerror')` listener and a context-level `'console'` one. `page.on('console')` on an ordinary page captures the same synthetic error correctly — the gap is service workers only, which is why the four `panel.*` suites' `panelErrors` checks were live all along (each fails under an injected `console.error` in the panel; verified per suite). `e2e`, `probe`, `highlight` and `picker` had ended on `assert.deepEqual(swErrors, [])` since M1–M5 against an array nothing could ever push to, and `e2e`'s was a bare assert outside every subtest as well, so its failure would have ended the outer test rather than reporting as one of its checks (Deviation 45's shape). `deep` was written last and had wrapped `console.error` itself; this is that wrapper lifted, so the twelfth browser suite inherits a working recorder instead of re-deriving a broken one. **Nothing real was hiding**: all six suites pass with the assertion live, and each fails when a synthetic `console.error` is injected into the worker — both directions run per suite on mutated copies. Two honest limits stay: anything logged between the worker starting and the recorder arming (milliseconds, no earlier hook exists for a worker target), and messages the BROWSER writes about the extension rather than the extension writing them itself ("Unchecked runtime.lastError") — those never pass through `console.error`, and `worker.on('console')` is the hook for them and is the hook that does not work. A worker that restarts is re-armed and the restart is COUNTED, because its predecessor's list dies with it and "no errors during any of this" cannot be claimed over a hole; `probe`'s deliberate relaunch calls `handoff()` before closing the context, which refuses if the outgoing worker is already unreadable. Still unclaimed, and stated rather than left implicit: the four `panel.*` suites drive the worker hard and check only the PANEL's console — no suite covers what the worker logs during a panel-driven probe. |
| 91 | M7 | **The three companion message types are handled OUTSIDE `routeMessage`, so an MCP agent cannot reach them.** §1.6 asks that an agent be able to do anything a person can. | Everything else the panel can ask, an agent can ask through the same router, and that identity is what makes parity structural rather than a promise. Pairing is the one place it must not hold: the thing on the far end of the socket is exactly what pairing decides to trust, so an agent that could re-run §12.3 would drop the socket, submit a code of its choosing and overwrite the stored token — the browser handing its access control to the party that control exists to check. `GET_COMPANION` and the broadcast could each be routed safely; they sit beside `PAIR_COMPANION` because a rule split across two tables is a rule someone re-derives wrongly later. This is a seventh §1.6 hole after the six recorded at M6, and the only one that is deliberate rather than unbuilt. |
| 92 | M7 | **`PAIR_FAIL` has two values where the panel asked for four** — wrong code, expired window, too many attempts and no window open are one answer. | `companion/src/pairing.js` hands the socket one indistinguishable `false` for all four on purpose and prints the detail on the terminal of the person who started it; that is MockLab's security boundary and the panel is not where it gets reopened. §11's copy for `REFUSED` therefore sends the reader to that terminal rather than guessing. `NO_COMPANION` is not a fifth cause leaking out: it is "no socket ever opened", which the extension observes locally and which any process able to attempt a pairing already sees at the transport. Its two causes — companion not running, companion running with no window — have the same remedy, and neither is "retype the code". |
| 93 | M7 | **`wsClient.isConnected()` now also requires the token the socket presented**, so an open-but-unauthenticated pairing socket reads as not connected. §10.5 never defined the dot, so this is a narrowing rather than a departure. | A pairing socket carries one frame each way; `hub.js` puts it in `pairingSockets` and never assigns it to `live`, `hub.call()` refuses with `not-connected` while `!live`, and `onFrame(..., authenticated=false)` answers `pair` and returns for everything else. Zero agent-visible work is possible over it, so "Connected — AI agents can control this site" would have been a small "Verified ✓". Verified against the companion rather than assumed. It also removes the flicker a successful pairing would otherwise show, at its cause rather than by suppressing status events — the case that matters is a companion dying while Settings is open, and "not that close, it doesn't count" is how that one gets lost too. What the dot can still be wrong about is unchanged and pre-existing: it stays green until the browser observes a close, and green means the channel to the companion, not that an AI client is attached to it. |
| 94 | M7 | **The real-hub test covers both live causes of `NO_COMPANION`, but the `new WebSocket` constructor-throw path is covered only by the fake socket.** | Established by mutation rather than by reading: removing the `onclose` settle fails the real-Chromium check, and removing the constructor-throw settle leaves all 14 passing. So both real causes — no listener, and a live hub refusing the upgrade at 401 with no pairing window — arrive through `ws.onclose`, exactly as the fake models them, and neither reaches the throw. That branch is a real Chrome behaviour on a URL it declines to dial; nothing in this suite produces it. Recorded as the honest boundary of the real-hub coverage rather than left implied by a green run. |

---

## For developers

No build step, no bundler, no TypeScript compile — plain ES2022 everywhere. Edit a
file, reload the extension, done.

```
npm install     # workspaces: extension, companion
npm test -ws    # node --test across both workspaces
npm run zip     # produces mocklab-extension.zip
```

`PLAN.md` is the full build specification. `BUILD_LOG.md` records what each milestone
delivered and how it was verified.
